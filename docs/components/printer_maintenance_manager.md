# Printer Maintenance Manager

The Printer Maintenance Manager component provides persistent tracking
of printer usage and scheduled maintenance intervals.

## Configuration

Add the following section to `moonraker.conf`:

```ini
[printer_maintenance_manager printer]
